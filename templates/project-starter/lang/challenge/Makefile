# Standard Interface for all projects

.PHONY: run test clean install

# Default to help
help:
	@echo "Available commands:"
	@echo "  make install   - Install dependencies"
	@echo "  make run       - Run the service locally"
	@echo "  make test      - Run tests"
	@echo "  make clean     - Clean up build artifacts"

install:
	@echo "TODO: Add installation commands (e.g., npm install, go mod download)"

run:
	@echo "TODO: Add run command (e.g., node src/index.js, go run main.go)"

test:
	@echo "TODO: Add test command"

clean:
	@echo "TODO: Add clean command"
